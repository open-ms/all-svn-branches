# CMake installation file
# Created 2008/10 during OpenMS
# retreat by CB && AB
#
#
#

PROJECT("OpenMS")
cmake_minimum_required(VERSION 2.6)

### all subdirectories of the root directory
ADD_SUBDIRECTORY(ANALYSIS)
ADD_SUBDIRECTORY(APPLICATIONS)
ADD_SUBDIRECTORY(CHEMISTRY)
ADD_SUBDIRECTORY(COMPARISON)
ADD_SUBDIRECTORY(CONCEPT)
ADD_SUBDIRECTORY(DATASTRUCTURES)
ADD_SUBDIRECTORY(EXAMPLES)
ADD_SUBDIRECTORY(FILTERING)
ADD_SUBDIRECTORY(FORMAT)
ADD_SUBDIRECTORY(KERNEL)
ADD_SUBDIRECTORY(MATH)
ADD_SUBDIRECTORY(METADATA)
ADD_SUBDIRECTORY(SYSTEM)
ADD_SUBDIRECTORY(TRANSFORMATIONS)
ADD_SUBDIRECTORY(VISUAL)

AUX_SOURCE_DIRECTORY(ANALYSIS ANALYSIS_SOURCES)
AUX_SOURCE_DIRECTORY(APPLICATIONS APPLICATIONS_SOURCES)
AUX_SOURCE_DIRECTORY(CHEMISTRY CHEMISTRY_SOURCES)
AUX_SOURCE_DIRECTORY(COMPARISON COMPARISON_SOURCES)
AUX_SOURCE_DIRECTORY(CONCEPT CONCEPT_SOURCES)
AUX_SOURCE_DIRECTORY(DATASTRUCTURES DATASTRUCTURES_SOURCES)
AUX_SOURCE_DIRECTORY(EXAMPLES EXAMPLES_SOURCES)
AUX_SOURCE_DIRECTORY(FILTERING FILTERING_SOURCES)
AUX_SOURCE_DIRECTORY(FORMAT FORMAT_SOURCES)
AUX_SOURCE_DIRECTORY(KERNEL KERNEL_SOURCES)
AUX_SOURCE_DIRECTORY(MATH MATH_SOURCES)
AUX_SOURCE_DIRECTORY(METADATA METADATA_SOURCES)
AUX_SOURCE_DIRECTORY(SYSTEM SYSTEM_SOURCES)
AUX_SOURCE_DIRECTORY(TRANSFORMATIONS TRANSFORMATIONS_SOURCES)
AUX_SOURCE_DIRECTORY(VISUAL VISUAL_SOURCES)

INCLUDE_DIRECTORIES(${OPENMS_SOURCE_DIR}../include/)

FIND_PACKAGE(Qt4)
SET(QT_USE_QTXML 1)

# bla add others, only QtCore and QtGui are enabled by default
INCLUDE(${QT_USE_FILE})

ADD_LIBRARY(OpenMS SHARED
	${ANALYSIS_SOURCES}
	${APPLICATIONS_SOURCES}
	${CHEMISTRY_SOURCES}
	${COMPARISON_SOURCES}
	${CONCEPT_SOURCES}
	${DATASTRUCTURES_SOURCES}
	${EXAMPLES_SOURCES}
	${FILTERING_SOURCES}
	${FORMAT_SOURCES}
	${KERNEL_SOURCES}
	${MATH_SOURCES}
	${METADATA_SOURCES}
	${SYSTEM_SOURCES}
	${TRANSFORMATIONS_SOURCES}
	${VISUAL_SOURCES}
)

