#

FILE(GLOB ui-sources . *.ui)
MESSAGE(${ui-sources})
#QT4_WRAP_UI(VISUAL_DIALOGS_UI_SOURCES ${ui-sources})
QT4_WRAP_UI_OWN(VISUAL_DIALOGS_UI_SOURCES include/OpenMS/VISUAL/DIALOGS/UIC/ ${ui-sources})


#MESSAGE(${VISUAL_DIALOGS_UI_SOURCES})

#INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
#FILE(GLOB ui-sources . *.ui)
#QT4_WRAP_UI(VISUAL_DIALOGS_UI_SOURCES ${ui-sources})
